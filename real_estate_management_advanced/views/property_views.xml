<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_property_tree" model="ir.ui.view">
        <field name="name">real.estate.property.tree</field>
        <field name="model">real.estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties">
                <field name="reference"/>
                <field name="name"/>
                <field name="property_type"/>
                <field name="city"/>
                <field name="status" widget="badge" decoration-success="status == 'rented'" decoration-info="status == 'available'" decoration-warning="status == 'reserved'" decoration-muted="status == 'sold'"/>
                <field name="price"/>
                <field name="rent_price"/>
            </list>
        </field>
    </record>

    <record id="view_property_form" model="ir.ui.view">
        <field name="name">real.estate.property.form</field>
        <field name="model">real.estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <header>
                    <field name="status" widget="statusbar" options="{'clickable': '1'}"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_contracts" type="object" class="oe_stat_button" icon="fa-book">
                            <field name="contract_count" widget="statinfo"/>
                        </button>
                        <button name="action_view_maintenance" type="object" class="oe_stat_button" icon="fa-wrench">
                            <field name="maintenance_count" widget="statinfo"/>
                        </button>
                    </div>
                    <field name="property_image" widget="image" class="oe_avatar"/>
                    <div class="oe_title">
                        <h1>
                            <field name="reference" readonly="1"/>
                        </h1>
                        <h2>
                            <field name="name" placeholder="Property Name"/>
                        </h2>
                    </div>
                    <group>
                        <group>
                            <field name="property_type"/>
                            <field name="price"/>
                            <field name="rent_price"/>
                            <field name="bedrooms"/>
                            <field name="bathrooms"/>
                            <field name="surface"/>
                            <field name="furnished"/>
                        </group>
                        <group>
                            <field name="owner_id"/>
                            <field name="tenant_id"/>
                            <field name="address"/>
                            <field name="city"/>
                            <field name="country_id"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <field name="description"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_property_kanban" model="ir.ui.view">
        <field name="name">real.estate.property.kanban</field>
        <field name="model">real.estate.property</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="id"/>
                <field name="name"/>
                <field name="status"/>
                <field name="property_type"/>
                <field name="city"/>
                <field name="property_image"/>
                <field name="reference"/>
                <field name="price"/>
                <field name="rent_price"/>
                <templates>
                    <t t-name="card">
                        <div class="row">
                            <div class="col-4">
                                <field name="property_image" widget="image" options="{'size': [90, 90]}"/>
                            </div>
                            <div class="col-8">
                                <strong class="o_kanban_record_title"><field name="name"/></strong>
                                <div><field name="reference"/></div>
                                <div><field name="property_type"/></div>
                                <div><field name="city"/></div>
                                <div class="mt-2">
                                    <field name="status" widget="label" decoration-success="status == 'rented'" decoration-info="status == 'available'" decoration-warning="status == 'reserved'" decoration-muted="status == 'sold'"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_property_search" model="ir.ui.view">
        <field name="name">real.estate.property.search</field>
        <field name="model">real.estate.property</field>
        <field name="arch" type="xml">
            <search string="Search Properties">
                <field name="name"/>
                <field name="city"/>
                <field name="property_type"/>
                <filter string="Available" name="available" domain="[('status', '=', 'available')]"/>
                <filter string="Rented" name="rented" domain="[('status', '=', 'rented')]"/>
                <filter string="Sold" name="sold" domain="[('status', '=', 'sold')]"/>
                <group expand="1" string="Group By">
                    <filter string="City" name="group_by_city" context="{'group_by': 'city'}"/>
                    <filter string="Type" name="group_by_type" context="{'group_by': 'property_type'}"/>
                    <filter string="Status" name="group_by_status" context="{'group_by': 'status'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_property" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">real.estate.property</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            Create your first property!
          </p>
        </field>
    </record>
</odoo>
